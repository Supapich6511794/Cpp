#include <iostream>
#include <cmath> //pow

int main()
{
    double x= 3.84;
    double y=4;
    double z;

    // z= std::max(x,y);
    // z= std::min(x,y);
    //z = pow(2,4);*****power ^
    //z= sqrt(9);****squerule 
    //z= abs(-3);*** Apserute ||
    //z = round(x);  round down or up depend on value
    //z= ceil (x); always round up
    //z= floor(x); always round down


    std::cout<<z;

    return 0;
}